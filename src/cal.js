import React from 'react';
import { json, checkStatus } from './util';

class Conv extends React.Component{
    constructor(props) {
        super(props);
        this.state = {
            base:null,
            compare:null,
            error:''
        };
    }
    
}